{% extends 'baseBack.html.twig' %}

{% block body %}
    <h1>Edit Ticket</h1>

    {{ form_start(form) }}
        {{ form_row(form.nombreMax) }}
        {{ form_row(form.disponibilite) }}
        <button type="submit" class="btn btn-primary">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_ticket_index') }}" class="btn btn-secondary">Back to list</a>
{% endblock %}