
{% extends 'baseBack.html.twig' %}
{% block title %}
<title>Article</title>
<style>
.table td:nth-child(3) {
        width: 200px;
        max-width: 200px;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
    }
   .table td:nth-child(5) {
        width: 200px;
        max-width: 200px;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
    }
    </style>

{% endblock %}


{% block body %}
    <h1>Article</h1>

   <table class="table" align="center" >
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>TitreArticle</th>
                <td>{{ article.titreArticle }}</td>
            </tr>
            <br>
            <tr>
                <th>DescriptionArticle</th>
                <br>
                <td >{{ article.descriptionArticle }}</td>
                 </br>            
            </tr>

            </br>
            <br>
            <tr>
                <th>DateArticle</th>
                <td>{{ article.dateArticle ? article.dateArticle|date('Y-m-d') : '' }}</td>
            </tr>
            </br>
            <tr>
                <th>ImageArticle</th>
                <td>
<img class="img-fluid" src="public/uploads/articles{{ article.imageArticle  }}"  >
</td>

                
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_article_index') }}"class="btn btn-primary">back to list</a>

    <a href="{{ path('app_article_edit', {'id': article.id}) }}"class="btn btn-primary">edit</a>

    {{ include('article/_delete_form.html.twig') }}
{% endblock %}
