{% extends 'baseBack.html.twig' %}
{% block title %}
<title>Categorie</title>
{% endblock %}


{% block body %}
<link href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet">
          <a href="{{ path('app_categorie_index') }}"class="material-icons"> arrow_back_ios</a>
    <h1>Categorie</h1>    <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}"class="material-icons">
edit
</a>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>NomCategorie</th>
                <td>{{ categorie.nomCategorie }}</td>
            </tr>
            <tr>
                <th>DescriptionCategory</th>
                <td>{{ categorie.descriptionCategory }}</td>
            </tr>
        </tbody>
    </table>



    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
