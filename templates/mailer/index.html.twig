<!DOCTYPE html>

<title>Email</title>

{% block body %}
<h1>Send email</h1>
    {{ form_start(form) }}
        {{ form_row(form.recipient) }}
       <button type="submit" onclick="return confirm('Are you sure you want to select this pack?')" style="background-color: #007bff; color: #fff; border: none; padding: 10px 20px; border-radius: 5px; margin-top: 20px;">confirmer</button>
    {{ form_end(form) }}

{% endblock %}
